steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - getmarketDetails_function
  - --source=.
  - --trigger-http
  - --runtime=python37
  - --allow-unauthenticated
  
 - name: 'gcr.io/cloud-builders/gcloud'
  args:
  - functions
  - deploy
  - pushDataToFireStore
  - --source=.
  - --trigger-http
  - --runtime=python37
  - --allow-unauthenticated
